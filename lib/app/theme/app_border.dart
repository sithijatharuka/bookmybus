import 'package:flutter/material.dart';

import 'app_colors.dart';

class AppBorder {
  AppBorder._();

  static BorderSide primary =
      const BorderSide(color: AppColors.border);

  static BorderSide divider =
      const BorderSide(color: AppColors.divider);
}